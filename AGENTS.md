# LK BIO 웹사이트 작업 규칙

## 프로젝트

- 별도 요청이 없으면 `index.html`만 사용하는 정적 사이트 구조를 유지합니다.
- `dev`는 검수 환경, `main`은 운영 환경입니다.
- 관련 없는 변경을 한 작업에 섞지 않습니다.

## 작업 흐름

1. 요청마다 GitHub Issue를 하나 만들고 아래 유형 라벨 하나를 붙입니다.
   - 오류: `type:bug`
   - 문구·이미지·연락처: `type:content`
   - UI·UX: `type:design`
   - 기능: `type:feature`
   - 배포·도메인·SEO·분석: `type:ops`
2. 최신 `dev`에서 `agent/<이슈번호>-<짧은-설명>` 브랜치를 만듭니다.
3. 작업 PR은 `dev`를 대상으로 만들고 본문에 `Refs #<이슈번호>`를 적습니다.
4. `dev` 병합 후 Cloudflare 배포 성공, HTTP 200, 데스크톱·모바일 UI와 주요 링크를 확인합니다. `dev`에는 `X-Robots-Tag: noindex`가 있어야 합니다.
5. 사람의 명시적 승인을 받은 뒤 `dev`에서 `main`으로 릴리스 PR을 만듭니다. 변경 요약, 검증 결과, 검수 주소와 관련 이슈의 `Closes #<이슈번호>`를 적습니다.
6. `main` 병합 후 운영 주소와 Cloudflare 배포 상태를 다시 확인합니다.

## 안전 원칙

- 사람의 승인 없이 `main`에 병합하지 않습니다.
- `main` 강제 푸시, 프로젝트 삭제·재생성, 도메인 변경, 비밀값 노출을 하지 않습니다.
- Cloudflare 작업은 공식 문서와 사용 가능한 Cloudflare Skill을 우선 사용합니다. MCP는 있으면 활용하는 선택 도구이며 저장소의 필수 조건이 아닙니다.
- 운영 링크와 환경 주소는 `README.md`를 기준으로 합니다.
